services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "5001:5000"
    environment:
      HOST: 0.0.0.0
      PORT: 5000
    tmpfs:
      - /app/backend/temp:size=64m
    restart: unless-stopped
